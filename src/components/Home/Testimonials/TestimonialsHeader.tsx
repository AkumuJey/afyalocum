import { Typography } from "@mui/material";

const TEstimonialsHeader = () => {
  return (
    <Typography
      variant="h4"
      component={`h2`}
      minWidth={`100%`}
      fontWeight={`bold`}
    >
      Testimonials
    </Typography>
  );
};

export default TEstimonialsHeader;
