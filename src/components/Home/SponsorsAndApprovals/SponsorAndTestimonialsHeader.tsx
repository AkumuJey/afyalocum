import { Typography } from "@mui/material";

const SponsorAndTestimonialsHeader = () => {
  return (
    <Typography
      component={`h4`}
      textAlign={`center`}
      gutterBottom
      variant="h4"
      fontWeight={`bold`}
    >
      We are approved by:
    </Typography>
  );
};

export default SponsorAndTestimonialsHeader;
