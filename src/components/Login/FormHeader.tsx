import { Typography } from "@mui/material";

const FormHeader = () => {
  return (
    <>
      <Typography
        variant="h4"
        fontWeight={`bold`}
        textAlign={`center`}
        color="secondary"
      >
        Login
      </Typography>
    </>
  );
};

export default FormHeader;
